allow_login_shell = false
approval_policy = "on-request"
approvals_reviewer = "auto_review"
model = "gpt-6-astra"
model_reasoning_effort = "ultra"
personality = "pragmatic"
sandbox_mode = "workspace-write"
service_tier = "priority"

[analytics]
enabled = false

# Atlassian plugin
# https://github.com/openai/plugins/tree/main/plugins/atlassian-rovo
[apps.connector_692de805e3ec8191834719067174a384]
default_tools_approval_mode = "approve"

# GitHub plugin
# https://github.com/openai/plugins/tree/main/plugins/github
[apps.connector_76869538009648d5b282a4bb21c3d157]
default_tools_approval_mode = "approve"

[apps.connector_76869538009648d5b282a4bb21c3d157.tools.github_create_repository]
approval_mode = "prompt"

[apps.connector_76869538009648d5b282a4bb21c3d157.tools.github_label_write]
approval_mode = "prompt"

[apps.connector_76869538009648d5b282a4bb21c3d157.tools.github_mark_all_notifications_read]
approval_mode = "prompt"

[apps.connector_76869538009648d5b282a4bb21c3d157.tools.github_merge_pull_request]
approval_mode = "prompt"

[apps.connector_76869538009648d5b282a4bb21c3d157.tools.github_unstar_repository]
approval_mode = "prompt"

# Datadog plugin
# https://github.com/openai/plugins/tree/main/plugins/datadog
[apps.asdk_app_69e8c7f174a08191a28b6da96c8062c4]
default_tools_approval_mode = "approve"

# Google Drive plugin
# https://github.com/openai/plugins/tree/main/plugins/google-drive
[apps.connector_5f3c8c41a1e54ad7a76272c89e2554fa]
default_tools_approval_mode = "approve"

# Notion plugin
# https://github.com/openai/plugins/tree/main/plugins/notion
[apps.asdk_app_69c18c28f1188191bf5b8445c4ab0a2e]
default_tools_approval_mode = "approve"

# OpenAI ChatGPT/Codex sites plugin
[apps.connector_20205bf7d4e99a89d7154bb849718324]
default_tools_approval_mode = "prompt"

# OpenAI Developers plugin
# https://github.com/openai/plugins/tree/main/plugins/openai-developers
[apps.connector_2de447f3f15448ebab48783d7e4f5d81]
default_tools_approval_mode = "approve"

# Slack plugin
# https://github.com/openai/plugins/tree/main/plugins/slack
[apps.asdk_app_69a1d78e929881919bba0dbda1f6436d]
default_tools_approval_mode = "approve"

# Vercel plugin
# https://github.com/openai/plugins/tree/main/plugins/vercel
[apps.connector_690a90ec05c881918afb6a55dc9bbaa1]
default_tools_approval_mode = "approve"

[apps.connector_690a90ec05c881918afb6a55dc9bbaa1.tools.vercel_deploy_to_vercel]
approval_mode = "prompt"

[apps.connector_openai_codex_document_control]
enabled = false

[features]
chronicle = false
fast_mode = true
js_repl = false
memories = true
prevent_idle_sleep = true

[marketplaces.openai-bundled]

[marketplaces.openai-primary-runtime]

[mcp_servers.context7]
url = "https://mcp.context7.com/mcp/oauth"

[mcp_servers.github]
bearer_token_env_var = "GITHUB_TOKEN"
default_tools_approval_mode = "approve"
url = "https://api.githubcopilot.com/mcp"

[mcp_servers.github.env_http_headers]
X-MCP-Toolsets = "GITHUB_TOOLSETS"

[mcp_servers.github.tools.create_repository]
approval_mode = "prompt"

[mcp_servers.github.tools.label_write]
approval_mode = "prompt"

[mcp_servers.github.tools.mark_all_notifications_read]
approval_mode = "prompt"

[mcp_servers.github.tools.merge_pull_request]
approval_mode = "prompt"

[mcp_servers.github.tools.unstar_repository]
approval_mode = "prompt"

# https://github.com/grafana/mcp-grafana
[mcp_servers.grafana]
command = "uvx"
args = [
  "mcp-grafana",
  "--enabled-tools",
  "assistant",
]
default_tools_approval_mode = "approve"
enabled = false
env_vars = [
  "GRAFANA_SERVICE_ACCOUNT_TOKEN",
  "GRAFANA_URL",
]

[mcp_servers.openai_developer_docs]
default_tools_approval_mode = "approve"
url = "https://developers.openai.com/mcp"

[mcp_servers.opentofu]
default_tools_approval_mode = "approve"
url = "https://mcp.opentofu.org/mcp"

[mcp_servers.sentry]
default_tools_approval_mode = "approve"
enabled = false
url = "https://mcp.sentry.dev/mcp"

[mcp_servers.spacelift]
default_tools_approval_mode = "approve"
url = "https://br3ndonland.app.spacelift.io/mcp"

[mcp_servers.spacelift.tools.intent]
approval_mode = "prompt"

[mcp_servers.spacelift.tools.mutate]
approval_mode = "prompt"

[mcp_servers.computer-use]
command = "./Codex Computer Use.app/Contents/SharedSupport/SkyComputerUseClient.app/Contents/MacOS/SkyComputerUseClient"
args = ["mcp"]
cwd = "."
enabled = false

[memories]
generate_memories = true
use_memories = true

# https://github.com/openai/plugins/tree/main/plugins/atlassian-rovo
[plugins."atlassian-rovo@openai-curated"]
enabled = false

# https://github.com/openai/plugins/tree/main/plugins/cloudflare
[plugins."cloudflare@openai-curated"]
enabled = false

# https://github.com/openai/plugins/tree/main/plugins/datadog
[plugins."datadog@openai-curated"]
enabled = false

# https://github.com/openai/plugins/tree/main/plugins/github
[plugins."github@openai-curated"]
enabled = true

# https://github.com/openai/plugins/tree/main/plugins/gmail
[plugins."gmail@openai-curated"]
enabled = false

# https://github.com/openai/plugins/tree/main/plugins/google-calendar
[plugins."google-calendar@openai-curated"]
enabled = false

# https://github.com/openai/plugins/tree/main/plugins/google-drive
[plugins."google-drive@openai-curated"]
enabled = false

# https://github.com/openai/plugins/tree/main/plugins/life-science-research
[plugins."life-science-research@openai-curated"]
enabled = false

# https://github.com/openai/plugins/tree/main/plugins/slack
[plugins."slack@openai-curated"]
enabled = false

# https://github.com/openai/plugins/tree/main/plugins/vercel
[plugins."vercel@openai-curated"]
enabled = true

# https://github.com/openai/plugins/tree/main/plugins/zotero
[plugins."zotero@openai-curated"]
enabled = false

[plugins."documents@openai-primary-runtime"]
enabled = true

[plugins."pdf@openai-primary-runtime"]
enabled = true

[plugins."presentations@openai-primary-runtime"]
enabled = true

[plugins."spreadsheets@openai-primary-runtime"]
enabled = true

[plugins."template-creator@openai-primary-runtime"]
enabled = true

[plugins."visualize@openai-bundled"]
enabled = true

[plugins."browser@openai-bundled"]
enabled = false

[plugins."chrome@openai-bundled"]
enabled = false

[plugins."codex-app-tools@openai-bundled"]
enabled = true

[plugins."computer-history@openai-bundled"]
enabled = false

[plugins."computer-use@openai-bundled"]
enabled = false

[plugins."unified-computer-use@openai-bundled"]
enabled = false

# project paths are parsed as plain strings, so absolute paths must be used.
# https://github.com/openai/codex/blob/8159f05/codex-rs/core/src/config_loader/mod.rs#L594-L605
# https://github.com/openai/codex/issues/11061
[projects."/Users/brendon/.dotfiles"]
trust_level = "trusted"

[projects."/Users/brendon/code"]
trust_level = "trusted"

[projects."/Users/bsmith/.dotfiles"]
trust_level = "trusted"

[projects."/Users/bsmith/code"]
trust_level = "trusted"

# writable_roots are parsed as a list of AbsolutePathBuf, so tilde expansion can be used.
# https://github.com/openai/codex/blob/main/codex-rs/utils/absolute-path/src/lib.rs
# https://github.com/openai/codex/blob/f628824/codex-rs/core/src/config/types.rs#L797
[sandbox_workspace_write]
exclude_slash_tmp = false
exclude_tmpdir_env_var = false
network_access = true
writable_roots = [
  "~/.bun",
  "~/.cache",
  "~/.cargo",
  "~/.claude",
  "~/.docker",
  "~/.local",
  "~/.npm",
  "~/.prefect",
  "~/.ruff_cache",
  "~/.rustup",
  "~/code",
  "~/Library/Caches",
  "~/Library/pnpm",
  "~/Library/Preferences/pnpm",
]

[shell_environment_policy]
inherit = "all"

[tui]
theme = "dracula-custom"

[tui.model_availability_nux]

[desktop]
agent-usage-reset-enabled = true
ambient-suggestions-enabled = true
appearanceDarkCodeThemeId = "dracula"
appearanceTheme = "dark"
appshotSoundEnabled = false
avatar-overlay-mascot-width-px = 80
codeFontSize = 14
composerEnterBehavior = "cmdAlways"
composerPlainTextMode = false
conversationDetailMode = "STEPS_COMMANDS"
defaultTerminalLocation = "right"
dock-icon-preference = "app-default"
followUpQueueMode = "steer"
git-branch-prefix = "br3ndonland/"
git-commit-instructions = """
- Do not create Git commits on `main`; always work on a feature branch.
- Do not Git push to `main`; always push to a feature branch.
- Format Git commit messages in the following style:

  ```text
  Imperative commit title limited to 50 characters

  Format commit messages as if they were Markdown.

  Begin by describing how the code works now and why a change is needed.
  The commit message body can be detailed. Full paragraphs are acceptable.
  Lines in commit message paragraphs should be limited to 72 characters.

  Summarize changes by saying "This commit will" and using the imperative.

  - The end of the commit message should have a list of references.
  - Add an unordered list item for each URL.
  - Do not hard wrap URLs. URLs can exceed 72 characters if needed.
  ```
"""
git-create-pull-request-as-draft = true
git-pr-instructions = """
- Limit the PR title to around 50 characters so it fits into a squash commit title.
- Write for a reviewer who has not read the conversation. Describe the final proposal and keep detail proportional to the change. Omit the implementation diary and abandoned approaches unless they explain a relevant tradeoff.
- Use plain, direct sentences. Minimize semicolons. Split sentences that join separate ideas or stack qualifications.
- Include a concise PR description with these sections:
  - `## Description`: explain the current behavior, problem, and reason for the change. Default to one short paragraph. Keep the proposed solution, implementation details, and resulting behavior in `## Changes`.
  - `## Changes`: explain the proposed approach in a short opening paragraph when useful, then summarize changes using imperative bullets prefaced with "This PR will:" (e.g. "Fix incorrect styling"). Group related changes by purpose and explain each change once. Add `Validation:` and `Notes:` within this section only when useful.
  - `## Related`: unordered list of links to related resources. Do not link the PR to itself.
- Summarize validation with the relevant checks, outcomes, and material gaps. Retain meaningful failures, skipped checks, and remaining verification. Summarize adversarial review outcomes without recounting each resolved finding. Include unresolved risks and waiver rationale when they affect the review decision. Place terminal output/log snippets in fenced code blocks inside HTML `<details><summary>...</summary> ... </details>` sections.
- State qualifications as concrete facts, limitations, or required actions that affect review or rollout. Preserve material uncertainty and migration requirements. Omit vague disclaimers and lists of things the PR does not do. For example, write "Configure the production domain in Vercel before sharing the demo" when that action is required, rather than "These are supported address formats, not a claim that we have added these domains to our projects."
- In the PR description, GitHub autolinked references should be used to refer to issues, PRs, commits, GitHub security advisories, and other supported links. GitHub permanent links to code snippets (permalinks) should be used when referencing code in the same repository as the PR. Permalinks should be on separate lines so they render properly. Non-GitHub URLs should be formatted as Markdown links with descriptive titles (no bare URLs).
- When updating a PR description (or issue body), fetch the current body first and merge changes into it rather than replacing the whole thing. Reviewers and integrations edit PR bodies out of band, and a full-replace `gh pr edit --body` silently overwrites those edits. Treat `gh pr edit --body` like an in-place rewrite: read the existing body, apply targeted changes (preserving reference-style links, footer metadata, and any prose you didn't author), then write back.
- Use the user's edits to an existing description as a style reference for subsequent updates. Before publishing, check that `## Description` contains only motivation, each change appears once, and every qualification gives the reviewer useful information.

Example of separating motivation from changes (section excerpt):

```markdown
## Description

Each customer currently requires a separate Node.js project and app in the repo. Maintaining these copies duplicates work.

## Changes

This PR proposes one shared project with configurations for each customer.

This PR will:

- Move the Node.js project to the repository root.
- Set customer configuration through `CUSTOMER_ID`.
```"""
git-pr-watch-auto-merge = false
git-pr-watch-instructions = """
For the pull request being watched, autonomously address existing and new review comments from humans and bots.

- Before making changes, verify the repository owner/name, PR number, head branch, current head SHA, and base branch. Work only on that PR's existing branch. Refresh remote state before pushing and preserve teammates' changes.
- Evaluate each comment against the current code. For valid, actionable findings, make focused fixes, run appropriate tests and required checks, commit, and push without asking me to repeat authorization.
- After confirming the fix is pushed, reply to the corresponding review thread with the commit reference and a concise explanation, then resolve that thread. Resolve already-fixed or superseded findings only after verifying them against the current head and explaining why. Leave unresolved or disputed findings open; do not resolve comments merely because GitHub marks them outdated.
- Preserve the PR's current draft/Ready for Review state. Never convert a Ready for Review PR back to draft, including while fixing comments or waiting for CI. Instructions to create new PRs as drafts do not apply to existing PRs.
- After each push, check whether the PR title and description still match the work. If changes are needed, fetch the current body and make targeted edits that preserve others' contributions and metadata.
- If the initial adversarial review has already been completed, do not repeat it for routine review-fix commits unless I explicitly request it.
- Check CI for the latest pushed commit and address failures caused by your changes. Report inaccessible checks or unrelated failures accurately; never claim unverified checks passed.
- Continue watching after fixes are pushed or checks turn green. Stop when the PR is merged or closed, or when I ask you to stop. Do not merge, close, deploy, change PR readiness, or start unrelated work.
- Stay quiet when nothing actionable has changed. Notify me concisely when fixes are pushed, a meaningful blocker needs my input, or monitoring ends. Avoid duplicate replies and notifications."""
git-pull-request-merge-method = "squash"
git-show-sidebar-pr-icons = true
hotkey-window-projectless-default-enabled = true
open-link-in-target-preference = "external-browser"
open-local-url-in-target-preference = "external-browser"
preventSleepWhileRunning = true
sansFontSize = 16
selected-avatar-id = "hoots"
toys-confetti-enabled = true
worktree-upstream-refresh-mode = "best-effort"

[desktop.appearanceLightChromeTheme]
accent = "#339cff"
contrast = 85
ink = "#0d0d0d"
opaqueWindows = false
surface = "#ffffff"

[desktop.appearanceLightChromeTheme.fonts]
code = "Rec Mono Duotone"

[desktop.appearanceLightChromeTheme.semanticColors]
diffAdded = "#00a240"
diffRemoved = "#ba2623"
skill = "#924ff7"

[desktop.appearanceDarkChromeTheme]
accent = "#ff79c6"
contrast = 85
ink = "#f8f8f2"
opaqueWindows = true
surface = "#282a36"

[desktop.appearanceDarkChromeTheme.fonts]
code = "Rec Mono Duotone"

[desktop.appearanceDarkChromeTheme.semanticColors]
diffAdded = "#50fa7b"
diffRemoved = "#ff5555"
skill = "#ff79c6"

[desktop.open-in-target-preferences]
global = "systemDefault"

[[skills.config]]
name = "github:gh-address-comments"
enabled = false

[[skills.config]]
name = "github:gh-fix-ci"
enabled = false
