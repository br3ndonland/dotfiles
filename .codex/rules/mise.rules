# Codex mise rules
#
# https://developers.openai.com/codex/rules
# https://github.com/bazelbuild/starlark/blob/master/spec.md
# The Codex VSCode extension can provide syntax highlighting (language ID codex-rules).
# Ruff can perform simple formatting. See scripts/ruff-rules.sh for commands.

# These rules allow mise-en-place commands.
# https://mise.jdx.dev/
prefix_rule(
    pattern=[
        "mise",
        [
            "lock",
            "run",
        ],
    ],
    decision="allow",
    justification="Allow agents to run mise tasks and manage lockfiles",
    match=[
        "mise lock python",
        "mise run check",
        "mise run format",
    ],
    not_match=[
        "mise self-update",
    ],
)
